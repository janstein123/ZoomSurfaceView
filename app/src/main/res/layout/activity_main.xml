<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.tct.zoomsurfaceview.MainActivity">

    <com.tct.zoomsurfaceview.CustomSurfaceView
        android:layout_width="match_parent"
        android:layout_height="256dp"
        android:id="@+id/zoom_surface_view"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/zoom_out_btn"
        android:layout_alignParentBottom="true"
        android:text="Zoom out"
        android:onClick="onZoomOutClicked"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/zoom_in_btn"
        android:layout_above="@+id/zoom_out_btn"
        android:text="Zoom in"
        android:onClick="onZoomInClicked"/>
</RelativeLayout>
